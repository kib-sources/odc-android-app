package npo.kib.odc_demo

/*
import android.content.Context
import androidx.test.ext.junit.runners.AndroidJUnit4
import androidx.test.platform.app.InstrumentationRegistry
import kotlinx.coroutines.runBlocking
import npo.kib.odc_demo.feature_app.data.db.BlockchainDatabase
import npo.kib.odc_demo.common.util.myLogs
import org.junit.Test
import org.junit.runner.RunWith

@RunWith(AndroidJUnit4::class)
class DbInstrumentedTest {

    private val appContext: Context = InstrumentationRegistry.getInstrumentation().targetContext

    @Test
    fun serverTest(): Unit = runBlocking {
        val banknotesDB = BlockchainDatabase.getInstance(appContext).banknotesDao()
        val res = banknotesDB.getBnidsAndAmounts()
        myLogs(res)
    }
}*/
