object CustomPlugins {

    const val prefix = "odc"

    const val androidApp = "${prefix}.android.application"
    const val androidAppCompose = "${prefix}.android.application.compose"
    const val androidLibrary = "${prefix}.android.library"
    const val androidLibraryCompose = "${prefix}.android.library.compose"
    const val androidFeature = "${prefix}.android.feature"
    const val androidTest = "${prefix}.android.test"
    const val androidHilt = "${prefix}.android.hilt"
    const val androidRoom = "${prefix}.android.room"
    const val androidAppFlavors = "${prefix}.android.application.flavors"
    const val jvmLibrary = "${prefix}.jvm.library"
}